# Story 1.2 — Setup Supabase (Projeto, Schema Base, RLS)

> **Epic:** 1 — Fundacao
> **Prioridade:** P0
> **Estimativa:** 1 dia
> **Agente:** @dev + @data-engineer
> **Depende de:** 1.1

---

## Descricao

Configurar projeto Supabase com schema base, policies RLS e client no monorepo.

## Acceptance Criteria

- [x] Projeto Supabase criado (ou Supabase local via Docker pra dev) — `supabase init` executado
- [x] Supabase CLI configurado (`supabase init`) — link pendente ate ter projeto hosted
- [x] Schema `public` com tabelas base: `tenants`, `profiles`, `subscriptions`
- [x] RLS habilitado em TODAS as tabelas
- [x] Policies RLS: usuario so ve dados do proprio tenant (via `get_user_tenant_id()`)
- [x] Supabase client configurado em `packages/db` (browser + service)
- [x] Tipos TypeScript manuais criados (auto-gen ao rodar Supabase local/hosted)
- [x] Script de seed com dados de teste
- [x] Migration inicial criada e versionada (00001_initial_schema.sql)
- [x] Supabase Storage bucket `documents` criado (na migration)
- [x] `.env.example` com `SUPABASE_URL` e `SUPABASE_ANON_KEY`

## Schema Base

```sql
-- Tenants (empresas)
CREATE TABLE tenants (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  cnpj VARCHAR(18) UNIQUE NOT NULL,
  razao_social TEXT NOT NULL,
  nome_fantasia TEXT,
  inscricao_estadual TEXT,
  segmento TEXT DEFAULT 'alimentos',
  regiao TEXT,
  capacidade_mensal INTEGER,
  created_at TIMESTAMPTZ DEFAULT now(),
  updated_at TIMESTAMPTZ DEFAULT now()
);

-- Profiles (usuarios vinculados a tenant)
CREATE TABLE profiles (
  id UUID PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE,
  tenant_id UUID REFERENCES tenants(id) ON DELETE CASCADE NOT NULL,
  full_name TEXT,
  role TEXT DEFAULT 'admin' CHECK (role IN ('admin', 'manager', 'operator', 'viewer')),
  created_at TIMESTAMPTZ DEFAULT now()
);

-- Subscriptions (planos)
CREATE TABLE subscriptions (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  tenant_id UUID REFERENCES tenants(id) ON DELETE CASCADE UNIQUE NOT NULL,
  plan TEXT DEFAULT 'free' CHECK (plan IN ('free', 'starter', 'pro', 'enterprise')),
  status TEXT DEFAULT 'active' CHECK (status IN ('active', 'cancelled', 'past_due', 'trialing')),
  trial_ends_at TIMESTAMPTZ,
  current_period_start TIMESTAMPTZ,
  current_period_end TIMESTAMPTZ,
  created_at TIMESTAMPTZ DEFAULT now()
);
```

## RLS Policies

```sql
-- Tenant isolation: usuario so ve dados do proprio tenant
CREATE POLICY "tenant_isolation" ON tenants
  FOR ALL USING (
    id IN (SELECT tenant_id FROM profiles WHERE id = auth.uid())
  );

CREATE POLICY "tenant_isolation" ON profiles
  FOR ALL USING (
    tenant_id IN (SELECT tenant_id FROM profiles WHERE id = auth.uid())
  );

CREATE POLICY "tenant_isolation" ON subscriptions
  FOR ALL USING (
    tenant_id IN (SELECT tenant_id FROM profiles WHERE id = auth.uid())
  );
```

## Notas Tecnicas

- Supabase local (Docker) pra desenvolvimento, hosted pra staging/prod
- Drizzle ORM configurado em `packages/db` como query builder
- Gerar tipos com `supabase gen types typescript --local > packages/db/types.ts`
- Todas as tabelas futuras DEVEM ter `tenant_id` + RLS

## File List

- [ ] `supabase/config.toml`
- [ ] `supabase/migrations/00001_initial_schema.sql`
- [ ] `supabase/seed.sql`
- [ ] `packages/db/src/client.ts`
- [ ] `packages/db/src/types.ts`
- [ ] `packages/db/drizzle.config.ts`
